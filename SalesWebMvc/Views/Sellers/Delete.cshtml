@model Seller

@{
    ViewData["Title"] = "Delete";
}

<h1>@ViewData["Title"]</h1>

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>Seller</h4>
    <hr />
    <dl class="row">
        <dt class="text-right">@Html.DisplayNameFor(model => model.Name)</dt>
        <dd class="text-left">@Html.DisplayFor(model => model.Name)</dd>

        <dt class="text-right">@Html.DisplayNameFor(model => model.Email)</dt>
        <dd class="text-left">@Html.DisplayFor(model => model.Email)</dd>

        <dt class="text-right">@Html.DisplayNameFor(model => model.BirthDate)</dt>
        <dd class="text-left">@Html.DisplayFor(model => model.BirthDate)</dd>

        <dt class="text-right">@Html.DisplayNameFor(model => model.BaseSalary)</dt>
        <dd class="text-left">@Html.DisplayFor(model => model.BaseSalary)</dd>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a asp-action="Index">Back to List</a>
        </form>
    </dl>
</div>